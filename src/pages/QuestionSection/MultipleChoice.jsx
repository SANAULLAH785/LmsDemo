// MultipleChoice.jsx

import React from "react";

const MultipleChoice = ({ content, onChange }) => {
  return (
    <div>
      <h3>Multiple Choice Component</h3>
      {/* <p>Content: {content}</p>
      <input
        type="text"
        value={content}
        onChange={(e) => onChange(e.target.value)}
      /> */}
    </div>
  );
};

export default MultipleChoice;
